import { Route } from '@angular/router';
import { CandidateCompareViewComponent } from './index';

export const CandidateCompare: Route[] = [
  {
    path: 'compare_candidate',
    component: CandidateCompareViewComponent
  }
];
